Agent integrated within the MCP server for service logic generation.

The developer must integrate an AI agent into this server so that when the server receives a request for WORKFLOW 2: LEADER INTERVIEW PROCESS, the agent is responsible for generating the complete state machine definition or service orchestration logic required to manage the workflow, including status transitions and email triggers.

**Planning:** In-service work: [`PLAN.md`](PLAN.md). Monorepo/compose/DB-seed follow-up (separate agent): [`PLAN_PLATFORM_FOLLOWUP.md`](PLAN_PLATFORM_FOLLOWUP.md).